[package]
name = "cargo-automod"
description = "a tool to automatically set up module structure for large projects"
version = "0.0.1-alpha"
edition = "2024"

[dependencies]
cargo_metadata = "0.23.1"
clap = { version = "4.6.1", features = ["derive"] }
regex = "1.12.3"
